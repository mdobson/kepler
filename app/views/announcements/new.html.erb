<fieldset class="span10">
	<legend>Create an Announcement</legend>
<%= form_for @post, :html=>{:class=>"form-horizontal well"}, :url => study_announcements_path do |postForm| %>
<div class="control-group">
  <%= postForm.label :title, :class=>"control-label" %>
  <div class="controls">
  	<%= postForm.text_field :title %>
  </div>
</div>

<div class="control-group">
	<%=  postForm.label :content, :class=>"control-label" %>
	<div class="controls">
		<%=  postForm.text_area :content, :size => "400x8", :style=>"width:461px;" %>
	</div>
</div>
<div class="form-actions">
  <%= postForm.submit "Create", :class => "btn btn-info" %>
</div>

<% end %>
</fieldset>